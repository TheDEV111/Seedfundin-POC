version: "1"
packages:
  - path: "internal/repository/db"
    queries: "db/queries"
    schema: "migrations"
    engine: "postgresql"
    emit_json_tags: true
    emit_prepared_queries: false
    emit_interface: true
    emit_exact_table_names: false
